enum ContentType {
  BlockQuote = 'block-quote',
  CaptionedImage = 'captioned-image',
  Embed = 'embed',
  EmbedTwitter = 'twitter',
  Image = 'image',
  Text = 'text',
  Title = 'title'
}

export default ContentType